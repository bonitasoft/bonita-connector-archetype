name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    - uses: actions/checkout@v2

    - name: Build and test archetype
      run: ./mvnw clean install
